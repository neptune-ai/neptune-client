name: Pull Request
on:
  pull_request:
jobs:
  pre-commit:
    runs-on: ubuntu-20.04  # Set back to ubuntu-latest after fixing it
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Pre-commit
        uses: ./.github/actions/pre-commit

  test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 75
    strategy:
      matrix:
        os: [ubuntu-20.04]  # Set back to ubuntu-latest after fixing it
        python-version: ["3.7", "3.10"] # minimum, maximum
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Tests
        uses: ./.github/actions/tests

  publish-tests:
    needs: [test]
    runs-on: ubuntu-20.04  # Set back to ubuntu-latest after fixing it
    if: always()
    permissions:
      checks: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Publish tests
        uses: ./.github/actions/publish-tests-result
